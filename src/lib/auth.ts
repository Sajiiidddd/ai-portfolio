// src/lib/auth.ts
export function getCurrentUserId(): string {
  // TODO: Replace with real auth logic later
  return 'anonymous-temp'; // This must match the `userId` you used when creating the comment
}
